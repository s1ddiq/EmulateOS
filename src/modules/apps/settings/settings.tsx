import AppWindow from "../components/app-window";

const SettingsApp = () => {
  return (
    <AppWindow title="Settings">
      <div>Settings</div>
    </AppWindow>
  );
};

export default SettingsApp;
